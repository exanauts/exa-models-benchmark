Fair re-run of the Rosenbrock AD comparison (tab:comparison), 2026-08-01
Host: shin-compute-001 (H1, 2x Xeon Gold 6130). Protocol:
  - single-thread rows: taskset -c 8, single-threading verified via
    process-CPU-time / wall-time ratio == 1.00
  - JAX/PyTorch hess includes extraction of solver-ready sparse values
    (N diag + N-1 subdiag) from the 3 color HVPs, gather maps precomputed
  - min over 50 reps after warmup; values verified against analytic
    tridiagonal Hessian (max abs err 0.0)
  - scripts: fair_rosenbrock.py (jax|torch), fair_rosenbrock_examodels.jl,
    fair_rosenbrock_examodels_mt.jl
  - versions: Julia 1.12.0, ExaModels v0.11.2 (v0.10.0 and main agree
    within noise), JAX 0.11.0 (clean venv), PyTorch 2.2.0 (system)

Single core (taskset -c 8):
  ExaModels  grad 1.20 ms   hess 2.71 ms   (v0.10.0; v0.11.2: 2.54, main: 2.67)
  JAX        grad 0.15 ms   hess 2.76 ms   cpu/wall 1.00
  PyTorch    grad 5.97 ms   hess 52.2 ms   cpu/wall 1.00

Multi-thread:
  ExaModels  grad 1.24 ms   hess 0.98 ms   (KernelAbstractions CPU backend, julia -t 32)
  JAX        grad 0.23 ms   hess 2.60 ms   cpu/wall 2.94 (XLA engages ~3 threads)
  PyTorch    grad 3.01 ms   hess 27.7 ms   cpu/wall 32.0

Why the published row was wrong: rosenbrock_examodels_timing.jl called
grad!(m, g, x0) / hess_coord!(m, vals, x0) -- NLPModels signatures put x
first, so it evaluated at uninitialized memory and wrote nnzh entries into
the nvar-length buffer (see the segfault in examodels_timing_output.txt).
The script is now fixed. Published JAX/PyTorch rows were additionally
multi-threaded (no caps) and stopped at raw HVP vectors.
